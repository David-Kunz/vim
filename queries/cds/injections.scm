; injections.scm

; Enable once it's actually used.
; (comment) @jsdoc
; (comment) @comment

; Hack for Unicode Escapes
(backtick_string) @javascript

; Text Blocks
(text_block language: (language_identifier) @language) @content
